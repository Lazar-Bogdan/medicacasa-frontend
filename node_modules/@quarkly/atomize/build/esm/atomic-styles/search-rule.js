import { hashPropsWithAliases } from '../constants/dict';
/**
 * color -> background-color
 * @param {*} strKey
 */

export var searchInKey = function searchInKey(strKey, accum) {
  var key = strKey.split('-');
  var searched = key.reduce(function (acc, item) {
    if (accum[acc]) {
      return acc;
    }

    acc = acc.replace(item + "-", '');
    return acc;
  }, strKey);
  return accum[searched] ? searched : false;
};
export var searchRuleInKey = function searchRuleInKey(strKey) {
  return searchInKey(strKey, hashPropsWithAliases);
};
export var searchBreakpointInKey = function searchBreakpointInKey(strKey, breakpoiints) {
  return searchInKey(strKey, breakpoiints);
};
export var searchEffectInKey = function searchEffectInKey(strKey, effects) {
  return searchInKey(strKey, effects);
};